# Docker environment configuration for Coordinator
# Loaded when kvdb.env=docker

# Coordinator server port
kvdb.coordinator.port=9000

# Health check configuration
kvdb.health.check.interval.seconds=10
kvdb.health.check.timeout.seconds=5
